1. List employees not belonging to department 30, 40, or 10.
SELECT ENAME FROM EMP WHERE DEPTNO NOT IN (10,30,40);

2. List the different designations in the company.
SELECT DISTINCT JOB FROM EMP;

3. List employees whose names either start or end with “S”.
SELECT ENAME FROM EMP WHERE ENAME LIKE "S%" OR "%S";

4. List employees whose names have letter “A” as second letter” in their names.
SELECT ENAME FROM EMP WHERE ENAME LIKE "_A%";

5. List the number of employees working with the company.
SELECT COUNT(*) AS "NUMBER OF EMPLOYEES" FROM EMP;

6. List the number of employees and average salary for employees in department 20.
SELECT COUNT(*) AS "NUMBER OF EMP IN DEPT 20", AVG(SAL) FROM EMP WHERE DEPTNO = 20;

7. List name, salary and PF amount of all employees. (PF is calculated as 10% of basic salary)
  SELECT ENAME, SAL, SAL*0.1 AS "PF" FROM EMP;

8. List names of employees who are more than 2 years old in the company.
SELECT ENAME FROM EMP WHERE DATEDIFF(NOW(), HIREDATE)/365.25 > 2;

9. List the employee details in the ascending order of their basic salary.
SELECT * FROM EMP ORDER BY SAL;

10. List the department numbers and number of employees in each department.
SELECT DEPTNO, COUNT(*) AS "NO OF EMP" FROM EMP GROUP BY DEPTNO;

11. List the total salary, maximum and minimum salary and average salary of the employees, for department 20.
SELECT SUM(SAL) AS "TOTAL SALARY", MAX(SAL), MIN(SAL), AVG(SAL) FROM EMP WHERE DEPTNO = 20;

12. Display the employees who have more salary as that of Smith
SELECT ENAME FROM EMP WHERE SAL > (SELECT SAL FROM EMP WHERE ENAME = "SMITH");

13. List the names of employees who are not eligible for commission.
 SELECT ENAME FROM EMP WHERE COMM IS NULL;
 
SET 2
1. Increment the salary of 7499 by 10% of his current salary.
UPDATE EMP SET SAL = SAL * 1.1 WHERE EMPNO = 7499;

2. Delete the Employee whose name starts P
DELETE FROM EMP WHERE ENAME LIKE 'P%';

3. Delete Employees who joined in Year 1980.
DELETE FROM EMP WHERE HIREDATE LIKE '1980%';

4. Increase the salary of Managers by 20% of their current salary.
UPDATE EMP SET SAL = SAL * 1.2 WHERE JOB = "MANAGER";


1st.


CREATE TABLE DEPT (DEPTNO INTEGER(5) PRIMARY KEY, DNAME VARCHAR(20), LOC VARCHAR(20));

CREATE TABLE EMP (EMPNO INTEGER(5) PRIMARY KEY, ENAME VARCHAR(20), JOB VARCHAR(20), MGR INTEGER(5), HIREDATE DATE, SAL INTEGER(10), COMM INTEGER(10), DEPTNO INTEGER(5), FOREIGN KEY (DEPTNO) REFERENCES DEPT(DEPTNO));

ALTER TABLE EMP ADD (AADHAAR INTEGER(12));
ALTER TABLE EMP MODIFY AADHAAR VARCHAR(12);

ALTER TABLE EMP DROP AADHAAR;

TRUNCATE TABLE DEPT;

DROP TABLE DEPT;

ALTER TABLE DEPT MODIFY DEPTNO INTEGER(5) AUTO_INCREMENT;

ALTER TABLE DEPT ADD FOREIGN KEY DEPTNO REFERENCES DEPT(DEPTNO);

CREATE INDEX IND ON EMP (SALARY);

CREATE VIEW V1 AS SELECT * FROM EMP WHERE DEPTNO = 20;
     
